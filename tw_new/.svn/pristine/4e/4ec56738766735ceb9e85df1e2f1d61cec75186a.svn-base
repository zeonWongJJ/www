package app.vdao.qidu.adapter;



/**
 * 只有一种viewType
 */

/*public class StoreListAdapter extends CommonAdapter<Store> {


    public StoreListAdapter(Context context) {
        super(context, R.layout.layout_store_item);
    }
    //设置定位经纬度
    private void setLocalLatLng(){

    }


    @Override
    protected void convert(ViewHolder holder, final Store store, int position) {
        holder.setText(R.id.store_name, store.getStore_name());
        int distance=(int)(store.getDistance());
        if(distance<1000){
            holder.setText(R.id.distance, distance+"m");
        }else {
            float  b   =  (float)(Math.round(distance/1000*100))/100;
            holder.setText(R.id.distance,b+"km");
        }

        holder.setText(R.id.store_introduction, store.getStore_introduction());
        holder.setText(R.id.store_address, store.getStore_address());

        ImageView storeImg=holder.getView(R.id.store_license);
        Glide.with(mContext).load("http://mpic.tiankong.com/f59/69b/f5969b153ac29b05f71c0ecf0cdf0509/640.jpg").into(storeImg);

        holder.getConvertView().setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {


            }
        });
    }
}*/
